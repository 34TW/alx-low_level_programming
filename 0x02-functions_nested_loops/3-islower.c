#include "main.h"

/**
* _islower- check if the character is lowercase
* @c: the character  
* Return: return 1 and 0 depend on the condition
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
