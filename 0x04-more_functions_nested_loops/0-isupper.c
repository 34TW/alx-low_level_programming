#include "main.h"

/**
 * _isupper - check if the character is uppercase
 * @c: the character c
 * Return: return 1 and 0 based on the condition
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
